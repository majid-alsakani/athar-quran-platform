# المساهمة في أثر

شكراً لاهتمامك بتطوير «أثر». هدفنا بناء منصة عربية عملية ومحترمة تساعد الحلقات القرآنية في الإدارة والمتابعة والتواصل. نرحب بالمساهمات البرمجية والتصميمية والتوثيقية ومراجعة النصوص العربية وتجارب الاستخدام.

## قبل أن تبدأ

يرجى البحث أولاً في Issues وPull Requests المفتوحة لتجنب تكرار العمل. إذا كان التغيير كبيراً أو يمس الصلاحيات أو البيانات أو تجربة أساسية، افتح Issue يشرح المشكلة والنتيجة المتوقعة قبل البدء بالتنفيذ. يضمن ذلك اتفاقاً مبكراً على النطاق.

## سير العمل

أنشئ Fork للمستودع، ثم أنشئ فرعاً قصير الاسم مثل `fix/attendance-state` أو `feat/guardian-report`. اجعل كل Pull Request مخصصاً لهدف واحد واضح. لا تضف مفاتيح وصول أو ملفات `.env` أو بيانات حقيقية لمستخدمين أو طلاب ضمن الكود أو اللقطات أو الاختبارات.

قبل فتح Pull Request شغّل الأمرين التاليين:

```bash
pnpm check
pnpm test
```

عند تعديل واجهة، تحقق من العربية واتجاه RTL وحالة الجوال وحالات التحميل والفراغ والخطأ. عند تعديل منطق الوصول أو بيانات الحلقة، أضف اختباراً يثبت حالة السماح وحالة المنع.

## معايير المراجعة

نراجع وضوح النطاق، سلامة الصلاحيات، جودة الاختبارات، اتساق RTL، وعدم إدخال بيانات شخصية أو أسرار. قد نطلب تقسيم Pull Request الكبير إلى تغييرات أصغر لتسهيل المراجعة والدمج.

## المساهمة الأولى

ابدأ بقضية موسومة بـ `good first issue` أو بتحسين صغير في التوثيق أو النصوص العربية أو اختبار محدد. اطرح أي سؤال في Issue؛ لا توجد مساهمة صغيرة إذا كانت تحسن وضوح المشروع أو موثوقيته.
